# Backlog — учебный антивирус

> Живой список задач и идей. Сделанное помечаем `[x]`, в работе — `[~]`, очередь — `[ ]`.
> Подробный контекст и архитектурные решения — в `CLAUDE.md`.

## Структура проекта

```
antivirus-project/
├── apps/              # рабочие программы и прототипы (каждая в своей подпапке)
│   ├── vtscan/        # этап 1 — CLI-сканер через VirusTotal
│   └── done/          # готовые сборки (vtscan.exe и т.п.)
├── backlog.md         # этот файл
├── requirements.txt   # общие зависимости (Python)
└── CLAUDE.md          # контекст проекта и roadmap
```

---

## Этап 1 — CLI-сканер VirusTotal ✅ ГОТОВО
- [x] SHA-256 файла/папки (`-r` рекурсивно)
- [x] Проверка по хэшу через VirusTotal API v3
- [x] `--upload` — догрузка неизвестных файлов на анализ
- [x] Вывод человекочитаемый / `--json`
- [x] Учёт лимита бесплатного ключа (пауза, `--delay`)
- [x] Код возврата 1 при находке вредоносного
- [x] Переезд в `apps/vtscan/`
- [x] Описание типа угрозы в выводе (троян/стилер/шифровальщик…) + семейство
- [x] Поддержка запуска из собранного `.exe` (поиск `.vtkey` рядом с exe)
- [x] Скрипт сборки `apps/vtscan/build_windows.bat` → кладёт `vtscan.exe` в `apps/done/`
- [ ] Собрать `vtscan.exe` на Windows-машине (запустить `build_windows.bat`)

## Этап 1.5 — «один файл» + кибер-терминальный интерфейс 🎯 БЛИЖАЙШЕЕ
Цель: удобно раздавать и запускать. Один `.exe` можно скинуть в Telegram, открыть на
Windows одним кликом — и он сразу работает, с красивым тёмным терминальным видом.

- [x] Интерактивный режим: запуск без аргументов (двойной клик) открывает
      кибер-терминал с вводом команд, а не argparse-ошибку
- [x] Команды: `scan`, `help`, `key`, `cd`, `clear`, `version`, `exit`
- [x] Prompt подстраивается под компьютер (реальная текущая папка)
- [x] Цветные вердикты через ANSI (ВРЕДОНОСНЫЙ — красный, ЧИСТО — зелёный…)
      + `colorama` для корректных цветов в консоли Windows
- [x] Первый запуск без ключа: спрашивает ключ VirusTotal и сохраняет рядом (`.vtkey`)
- [x] Правый клик по файлу → «Проверить VT-сканером» — регистрируется автоматически
      при каждом запуске (реестр HKCU без админки; флаг `--pause` держит окно открытым)
- [x] Авто-обновление: команда `check-update` (скачивает свежий релиз и заменяет exe)
      + тихая проверка новой версии при старте. Релизы публичные → работает у друзей/семьи.
- [x] Неизвестный файл (нет в базе VT) → терминал сам предлагает загрузить на анализ (`--upload`)
- [x] Сборка одного `.exe`: локально `build_windows.bat` ИЛИ авто в облаке (GitHub Actions)
- [ ] (опц.) Инсталлятор `setup.exe` (Inno Setup/NSIS) с ярлыком в «Пуск»
      — но для «скинул-открыл-запустил» хватает портативного onefile
- [x] Дизайн-макет терминального интерфейса (согласован визуально)

## Этап 2 — мульти-источник: локальные движки + несколько баз
Цель: вердикт не по одному VirusTotal, а агрегированный по нескольким независимым
источникам — так точнее и устойчивее к пропускам отдельного движка.

> ⚠️ КРИТЕРИЙ (не нарушать): всё работает из ОДНОЙ папки. Пользователь скачивает
> одну нашу программу — и она работает. ClamAV и др. локальные движки **бундлятся
> или авто-провижнятся в подпапку рядом с программой** (`<папка_exe>/engines/...`),
> НЕ ставятся отдельно и НЕ разбросаны по системе. Базы качаем в папку приложения.
> Подробнее: память `single-folder-constraint`.

- [x] Общий интерфейс «движок проверки» (`engines.py`: EngineResult) + агрегатор вердиктов
- [x] VirusTotal как источник в разбивке (`_vt_engine_result`); логика VT в `_scan_virustotal`
- [x] ClamAV-движок (обёртка над `clamscan`, `engines.py`) — протестирован на Mac имитацией
- [x] Разбивка вердикта по источникам в выводе (├ VirusTotal ● … └ ClamAV ● …)
- [x] Тест на EICAR: VirusTotal + ClamAV(stub) → агрегированный вердикт ВРЕДОНОСНЫЙ
- [ ] **Авто-провижн ClamAV в папку приложения** (команда `setup-clamav`): скачать
      портативный clamscan для Windows + базу сигнатур в `<папка_exe>/engines/clamav/`.
      Критерий «одна папка». Тестировать на Windows (нет доступа с Mac).
- [ ] YARA — локальные правила (эвристика по содержимому файла), база в папке приложения
- [ ] Офлайн-режим: базовый вердикт без интернета (ClamAV + YARA)

### Открытые базы/источники для подключения (бесплатные)
- [ ] MalwareBazaar (abuse.ch) — поиск по SHA-256, бесплатный API-ключ
- [ ] MetaDefender Cloud (OPSWAT) — мультидвижковая проверка по хэшу, бесплатный тариф
- [ ] MalShare — база хэшей вредоносов, бесплатный API-ключ
- [ ] Hybrid Analysis — репутация/анализ по хэшу, бесплатный API-ключ
- [ ] AlienVault OTX — Open Threat Exchange, репутация/IOC, бесплатно
- [ ] ThreatFox (abuse.ch) — индикаторы компрометации по хэшу
- [ ] NSRL (whitelist) — база известных «хороших» хэшей, гасит ложные срабатывания
> У каждого источника свои лимиты и формат ответа. Агрегатор приводит их к единому
> виду и считает итог, напр.: «N из M источников считают файл опасным».

## Этап 3 — фоновое наблюдение (Python, без драйвера)
> Сюда же ложатся одобренные «фишки безопасности» (см. раздел ниже): монитор
> автозапуска ⭐, ловушка-шифровальщик, поведенческая эвристика, USB-скан и др.
- [ ] `watchdog` — слежение за новыми файлами (Downloads и др.) → автопроверка
- [ ] `psutil` — мониторинг процессов (CPU, сеть, родитель, путь к exe)
- [ ] Поведенческая эвристика: признаки → score → уведомление
- [ ] Возможность приостановить (suspend) подозрительный процесс из user-mode
- [ ] Упаковка как фоновый сервис / трей-приложение
- [ ] **Download-gatekeeper (прагматичная версия):** ловить новый файл в `Downloads`
      сразу при появлении → сканировать движками этапов 1–2 → если вредоносный,
      мгновенно изолировать (карантин/блок доступа) ДО того, как пользователь откроет.
      Это не «пауза посреди скачивания», но практический эффект тот же.
      - [ ] Опционально: расширение для браузера (Chrome/Edge downloads API),
            которое ставит загрузку на паузу до вердикта — настоящая блокировка,
            но только внутри браузера.

## Этап 3.5 — download-gatekeeper (идея пользователя, разнесена по этапам)
> Хотим: скачиваемый файл проверяется ДО завершения загрузки, и докачивается/
> становится доступным только после одобрения. Три пути реализации:
> 1. **Расширение браузера** (downloads API) — пауза загрузки до вердикта. User-mode,
>    реалистично, но только для поддержанных браузеров. → можно сделать в рамках этапа 3.
> 2. **Локальный MITM-прокси** — скан файла до отдачи браузеру. Требует вскрытия HTTPS
>    и своего сертификата. Тяжело, для учебного этапа избыточно. → отложено.
> 3. **Minifilter-драйвер** — настоящая блокировка открытия/записи файла для любой
>    программы в реальном времени. → это этап 4 (см. ниже).

## Этап 4 — резидентная реал-тайм защита (на потом)
- [ ] Minifilter-драйвер ядра на C/C++ (WDK) — перехват открытия файлов
      (это и есть «настоящий» download-gatekeeper из этапа 3.5: блокировка
      доступа к файлу до вердикта для любой программы, не только браузера)
- [ ] Канал «драйвер → Python-сервис» для передачи событий
- [ ] EV-сертификат для подписи драйвера, регистрация у Microsoft

---

## Фишки безопасности (чем выделиться) — ✅ одобрено, разложено по этапам
> Почти всё одобрено пользователем. Метка [этап N] показывает, куда ложится.
> Большинство — фоновый режим (этап 3), user-mode, без драйвера ядра.

- [ ] **Монитор автозапуска (persistence)** [этап 3] — снимок Run-ключей реестра,
      папки «Автозагрузка», Планировщика, Служб → алерт при появлении нового
      (там прячется ~90% малвари). ⭐ ПРИОРИТЕТ. Смыкается с мониторингом процессов.
- [ ] **Ловушка от шифровальщиков (canary)** [этап 3] — несколько маленьких файлов-
      приманок на папку (килобайты, не пустые), процесс начал их массово менять →
      заморозить. Нагрузки на диск/память нет. Реальная защита от ransomware (`watchdog`).
- [ ] **Поведенческая эвристика процессов** [этап 3] — следим за поведением, а не файлом:
      документ запускает PowerShell/cmd, exe из Temp/Downloads лезет в сеть, неподписанный
      плодит процессы → «очки подозрительности» → алерт/заморозка. Лёгкая (`psutil`).
- [ ] Авто-скан флешек/USB при подключении [этап 3].
- [ ] Проверка ссылок (анти-фишинг) по URLhaus/PhishTank — до клика [этап 3].
- [ ] Монитор сетевых подключений + подсветка «плохих» адресов (ThreatFox) [этап 3].
- [ ] Контроль целостности критичных файлов (tripwire): `hosts`, DNS, системные [этап 3].
- [ ] **«Семейный режим»** — отчёты человеческим языком + авто-сканы по расписанию [этап 3].
- [ ] Приватность как фича: всё локально, наружу только хэши в выбранные базы [принцип].

### Защита самой программы (когда станет фоновой, этап 3–4)
- [ ] Подпись кода (code signing) — целостность + снятие предупреждения SmartScreen.
- [ ] Самопроверка целостности при старте (свой хэш).
- [ ] Запуск как защищённый сервис; изоляция настроек/карантина по пользователям.
> Прятать сам код бессмысленно (Python-сборку можно декомпилировать). Защищаем не код,
> а секреты (их в программе нет) и возможность отключить/подменить защиту.

## Идеи / разное (без привязки к этапу)
- [ ] Кэш результатов по хэшу (локальная БД), чтобы не тратить лимиты источников
- [ ] Логи сканирований в файл
- [x] Авто-сборка Windows-`.exe` через GitHub Actions.
- [x] Репозиторий публичный + Releases по тегам (`v*`) с прикреплённым `.exe`.
      Безопасно: ключ в `.vtkey`/`.gitignore`, в репо и exe не попадает (проверено).
- [ ] Инсталлятор `setup.exe` (Inno Setup/NSIS) — делать на этапе 3–4, когда появится
      фоновый сервис: автозапуск, ярлыки, деинсталлятор. Сейчас портативный exe удобнее.
- [ ] **Отдельное приложение в стиле кибер-терминала** (как мокап, что рисовали:
      тёмно-синий/чёрный фон, моноширинный шрифт, цветные вердикты, рамки-источники).
      ВАЖНО: не буквально cmd, а самостоятельное красивое окно, лишь стилизованное под cmd.
      Кандидаты: web-UI (pywebview/Electron-подобное) или Python-GUI с тёмной темой.
      Сейчас работаем в реальном cmd; это — полировка внешнего вида на будущем этапе.
- [ ] Трей-иконка/фоновый режим поверх движков (этап 3).
