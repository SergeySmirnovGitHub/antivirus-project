# API-ключ — НИКОГДА не коммитить!
.vtkey
*.key
# Ключи доп. источников (MalwareBazaar/OTX/Kaspersky) — секреты, не коммитить!
keys.json
apps/*/keys.json

# Данные приложения в dev-режиме (лежат рядом со скриптом) — не в git
apps/vtscan/engines/
whitelist.json
apps/*/whitelist.json
selftest-sample.txt
apps/*/selftest-sample.txt
*.quarantine

# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# Артефакты сборки PyInstaller
build/
dist/
*.spec

# Отчёты сканера
*.scan.json

# IDE
.vscode/
.idea/

# macOS
.DS_Store

# Локальные настройки Claude Code (машинно-зависимые)
.claude/

# Тестовые вредоносные образцы — не храним в репозитории
samples/
*.eicar

# Собранные exe (артефакты, в git не храним)
apps/done/*.exe

# Карантин (рантайм, не в git)
quarantine/
apps/*/quarantine/

# Тестовые EICAR-файлы (синкаются, но не в git)
apps/test-samples/eicar-*.txt
