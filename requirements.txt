requests>=2.31.0
colorama>=0.4.6
# GUI-окно (этап 3). На Windows использует встроенный WebView2.
pywebview>=5.0
# Фоновое наблюдение (этап 3): процессы и слежение за файлами.
psutil>=5.9
watchdog>=3.0
# Системные уведомления (всплывают справа). Только Windows.
windows-toasts>=1.1.0; sys_platform == "win32"
# Трей-иконка фоновой защиты (этап 3): значок в системном лотке + меню.
pystray>=0.19
Pillow>=10.0
